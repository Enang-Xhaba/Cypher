{
  "name": "cypher-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "Cypher decode playground with multiple ciphers and tests",
  "scripts": {
    "test": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}